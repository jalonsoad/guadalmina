
 
    <div class="container">
       <div class="py-2 text-center">
        <img class="d-block mx-auto mb-4" src="https://www.guadalminagolf.com/imggolf/logoguadalmina.png" alt="logo guadalmina" >
        <h2><%= t(:title).upcase %></h2>
        <p class="lead"><%= t(:paragraph1) %></p>
      </div>


    <div class="my-3 p-3 bg-white rounded box-shadow">
        <h6 class="border-bottom border-gray pb-2 mb-0"><%= t(:mainpoints) %></h6>
        <div class="media text-muted pt-3">
          <img data-src="holder.js/32x32?theme=thumb&bg=007bff&fg=007bff&size=1" alt="" class="mr-2 rounded">
          <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
            <strong class="d-block text-gray-dark"><%= t(:transparency) %></strong>
            <%= t(:transparencytext) %>
          </p>
        </div>
        <div class="media text-muted pt-3">
          <img data-src="holder.js/32x32?theme=thumb&bg=e83e8c&fg=e83e8c&size=1" alt="" class="mr-2 rounded">
          <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
            <strong class="d-block text-gray-dark"><%= t(:order) %></strong>
            <%= t(:ordertext) %>
          </p>
        </div>
        <div class="media text-muted pt-3">
          <img data-src="holder.js/32x32?theme=thumb&bg=6f42c1&fg=6f42c1&size=1" alt="" class="mr-2 rounded">
          <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
            <strong class="d-block text-gray-dark"><%= t(:admission) %></strong>
            <%= t(:admissiontext) %>
          </p>
        </div>
       
      </div>

      <div class="row">
        <div class="col-md-4 order-md-2 mb-4">
          <h4 class="d-flex justify-content-between align-items-center mb-3">
            <span class="text-muted"><%= t(:totalvotes) %></span>
            <span class="badge badge-secondary badge-pill"><%= @allvotes.count %></span>
          </h4>
          <span class="text-muted"><%= t(:lastvoters) %></span>
          <ul class="list-group mb-3">
            <div class="text-success">
              <% @lastfivevotes.each do |v| %>
                <li class="list-group-item d-flex justify-content-between lh-condensed">
                <div>
                  <h6 class="my-0">Socio <%= v.member %></h6>
                   <small class="text-muted"><%= v.full_name %></small>
                </div>
                <span class="text-muted"><%= v.created_at.strftime("%d/%m/%Y %H:%M") %></span>
              </li>

              <% end %>
            
          </div>
            
          </ul>

        
        </div>

        <div class="col-md-8 order-md-1">
          <h4 class="mb-3"> <%= t(:getyourdata) %></h4>


  <%= flash_messages %>



        <%= bootstrap_form_for(Vote.new ,label_errors: true, inline_errors: true) do |f| %>
      
            <div class="row">
              <div class="col-md-6 mb-3">
                <%= f.text_field :first_name,  label:  t(:firstname), help: t(:mandatory)  %>
                <%= f.alert_message "Please fix the errors below." %>
                <div class="invalid-feedback">
                  EL nombre es obligatorio
                </div>
              </div>
              <div class="col-md-6 mb-3">
                 <%= f.text_field :last_name,  label: t(:lastname) , help: t(:mandatory) %>
                <div class="invalid-feedback">
                  Valid last name is required.
                </div>
              </div>
            </div>

            <div class="row">
               <div class="col-md-6 mb-3">
                   <%= f.text_field :member,  label:  t(:member), help: t(:mandatory)  %>
                  <div class="invalid-feedback">
                    Valid last name is required.
                  </div>
                </div>
                <div class="col-md-6 mb-3">
                  <%= f.text_field :cif,  label: "CIF/NIE", help:  t(:mandatory) %>
                  <div class="invalid-feedback">
                    Valid first name is required.
                  </div>
                </div>
               
              </div>


              <label><%= t(:alldata) %></label>
           

          
           
                <%= f.button   t(:senddata), :class=> "btn btn-primary btn-lg btn-block text-center" %>

      <% end %>
        </div>
      </div>


 

     <hr class="mb-4">

       <main role="main" class="col-md-9 ml-sm-auto col-lg-12">

          <h2><%= t(:members) %></h2>
          <div class="table-responsive">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                  <th>#</th>
                  <th><%= t(:firstname) %></th>
                  <th><%= t(:lastname) %></th>
                  <th><%= t(:member) %></th>
                  <th><%= t(:date) %></th>
                </tr>
              </thead>
              <tbody>
                <% @allvotes.each do |v| %>
                <tr>
                  <td><%= v.id %></td>
                  <td><%= v.first_name.capitalize %></td>
                  <td><%= v.last_name.capitalize %></td>
                  <td><%= v.member %></td>
                  <td><%= v.created_at.strftime("%d/%m/%Y  %H:%M") %></td>
                </tr>
                <%end %>
               
              </tbody>
            </table>
          </div>
        </main>

  </div>

   
    </div>



      <!--CHECK OUT  Placed at the end of the document so the pages load faster -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script>
      // Example starter JavaScript for disabling form submissions if there are invalid fields
      (function() {
        'use strict';

        window.addEventListener('load', function() {
          // Fetch all the forms we want to apply custom Bootstrap validation styles to
          var forms = document.getElementsByClassName('needs-validation');

          // Loop over them and prevent submission
          var validation = Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
              if (form.checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
              }
              form.classList.add('was-validated');
            }, false);
          });
        }, false);
      })();
    </script>